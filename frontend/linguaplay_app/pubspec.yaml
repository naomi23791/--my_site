name: linguaplay_app
description: "LinguaPlay - Apprenez les langues en jouant"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'  # Changé à 3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  google_fonts: ^6.1.0
  flutter_local_notifications: ^17.1.2
  confetti: ^0.7.0
  timezone: ^0.9.2
  flutter_native_timezone_updated_gradle: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/vocab_en.json